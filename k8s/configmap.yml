apiVersion: v1
kind: ConfigMap
metadata:
  name: url-shortener-config
  namespace: url-shortener

data:
  PORT: "3000"
  BASE_URL: "http://localhost"
  MONGO_URI: "mongodb://mongo-service:27017/urlShortener"
